# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
venv/
.venv/

# Application data
data/
*.db
*.sqlite3

# Secrets & credentials — never commit these (mount them on the host instead)
# Traefik and Authelia now live in ../cumulus/edge; these paths are ignored
# wholesale so credentials can never be re-added here by accident.
authelia/
traefik/

# ACME account key + certificate private keys (stale copy; live store is
# ../cumulus/edge/letsencrypt)
letsencrypt/

# OS / editor
.DS_Store
*.swp
